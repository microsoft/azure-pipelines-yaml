{
  "iconName": "docker",
  "parameters": [
    {
      "name": "dockerfilePath",
      "type": "string",
      "required": "true",
      "displayName": "Dockerfile",
      "defaultValue": "**/Dockerfile"
    }
  ]
}